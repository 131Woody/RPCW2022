extends layout

block content
    h1(class="w3-container w3-blue") Lista de Músicas
    table(class="w3-table-all")
        tr(class="w3-green")
            th Identificador
            th Titulo
            th Provincia
        each e in lista
            tr 
                td
                    a(href="/musicas/#{e.id}")=e.id
                td=e.tit
                td=e.prov